import React from "react";

const Nav = () => {
  return (
    <div>
      {/* Logo */}
      {/* Favourites Section : store the watchlist movies into localstorage  */}
      {/* search Section */}
    </div>
  );
};

export default Nav;
